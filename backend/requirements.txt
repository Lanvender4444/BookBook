fastapi>=0.111.0
uvicorn[standard]>=0.29.0
sqlalchemy>=2.0.0
anthropic>=0.28.0
httpx>=0.27.0
google-generativeai>=0.7.0
sse-starlette>=1.8.0
getmac>=0.9.0
python-dotenv>=1.0.0
aiofiles>=23.0.0
pydantic>=2.0.0
ebooklib>=0.18
python-docx>=1.1.0
reportlab>=4.0.0
pypdf>=4.0.0
python-multipart>=0.0.9
Pillow>=10.0.0
# 可选：让 PDF/DOCX 导出能内嵌 SVG 图（纯 Python，无系统依赖）。缺失时自动降级为图说明文字。
svglib>=1.5.0
# 向量数据库：LanceDB —— 嵌入式、单目录、全平台 wheel（Win/Mac/Linux），
# 原生支持 向量检索 + 全文 BM25(Tantivy) + 内置 RRF 混合重排。
# 缺失时 rag_service 自动回落到纯 Python（BM25 + 暴力余弦 + RRF）。
lancedb>=0.13.0
pyarrow>=14.0.0
# 分块：langchain 的递归分隔符切分器（只装这个子包，纯 Python、全平台，不拉整个 langchain）。
# 缺失时 rag_service 自动回落到手写分块。
langchain-text-splitters>=0.2.0
